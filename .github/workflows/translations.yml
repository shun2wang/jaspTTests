on:
  schedule:
    - cron:  '30 2 * * 6'
    
  workflow_dispatch:

name: generate-translations

jobs:
  generate-translations:
    runs-on: ubuntu-latest
    env:
      REPO_KEY: ${{ secrets.github.token }}
      WEBLATE_KEY: ${{ secrets.WEBLATE_KEY }}
      QML_REPO: jaspttests-qml
      R_REPO: jaspttests-r

    steps:
      # - uses: jasp-stats/jasp-actions/wlcLock@master

      - uses: shun2wang/jasp-actions/translations@qt64
